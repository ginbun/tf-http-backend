DATABASE_URL=postgres://tf_backend:secret@localhost:5432/tf_backend
LISTEN_ADDR=0.0.0.0:8080
DB_MAX_CONNECTIONS=20
RUST_LOG=tf_http_pg_backend=info,info

# Optional basic auth (set both or neither)
# HTTP_BASIC_USERNAME=tofu
# HTTP_BASIC_PASSWORD=change-me
